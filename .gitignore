__pycache__/
*.pyc
.cache.sqlite
venv/
.venv/

# Built by generar_web.py on every workflow run; published straight to GitHub Pages via
# actions/deploy-pages instead of being committed (was the single biggest contributor to
# repo growth at ~3MB/commit).
docs/

# A manual export of the adequacy report that kept every forecast vintage (~106 publishes per
# delivery hour) instead of just the latest -- 444 MB, which GitHub blocks outright at 100 MB.
# data/ieso_adequacy.csv holds the same period in ~5 MB; see src/update_adequacy.py.
data/ieso_adequacy_forecast.csv
